// Importar Buffer para ingresar por teclado

// public class Ej_2 {
    // Declarar variable char y variable entero;
    // try
        // Realizas el Buffer para teclado;
        // Pedir al usuario que ingrese un carácter dígito o carácter letra minúscula;

        // Realizar un while con las condiciones "variable char >= 'a' && char <= 'z' or var int >= 0 && var int <= 9" {
            // Imprimir dicho dígito o letra

            // Realizar un if si es letra vocal: letra == 'a' o letra == 'e' ... {
                // Imprimir dicho carácter
                // } Realizar un else para las consonantes {
                    // Imprimir dicho carácter
                //}
        //}
    //} catch {

    //}
//}
